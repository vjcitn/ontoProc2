# Retrieve the ontology prefix from a SemsqlConn

Retrieve the ontology prefix from a SemsqlConn

## Usage

``` r
get_prefix(x, ...)
```

## Arguments

- x:

  A `SemsqlConn` object.

## Value

character(1) the primary ontology prefix (e.g. `"CL"`).
